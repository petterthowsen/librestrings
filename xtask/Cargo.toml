[package]
name = "xtask"
description = "Bundles the plugin (cargo xtask bundle strings-plugin --release) or bundles and installs it to ~/.clap (cargo xtask install)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
nih_plug_xtask.workspace = true
