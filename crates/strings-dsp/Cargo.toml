[package]
name = "strings-dsp"
description = "Physical-modeling DSP for bowed strings: waveguide strings, bow friction, analysis helpers"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
