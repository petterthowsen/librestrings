[package]
name = "strings-render"
description = "Offline renderer: drives strings-dsp models and writes WAV/CSV"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
strings-dsp.workspace = true
clap = { version = "4.5", features = ["derive"] }
hound = "3.5"
